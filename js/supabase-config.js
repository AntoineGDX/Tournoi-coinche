// C'EST COINCHÉ — configuration Supabase
// Remplace ces deux valeurs par celles de ton projet :
// Supabase → Settings → API → "Project URL" et "anon public" key.
// L'anon key est faite pour être publique (la sécurité vient des règles RLS
// définies dans supabase/schema.sql) — pas de souci à la committer.
const SUPABASE_URL = 'https://YOUR-PROJECT.supabase.co';
const SUPABASE_ANON_KEY = 'YOUR-ANON-PUBLIC-KEY';
